{
  "name": "highcharts-stream",
  "description": "streaming interface to highcharts-browserify. stream.pipe(chart) === happy",
  "version": "0.0.1",
  "repository": {
    "url": "git://github.com/soldair/highcharts-stream.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js -o bundle.js ",
    "test": "npm run build && tape test/*.js",
    "prepublish": "npm run build"
  },
  "author": "Ryan Day",
  "dependencies": {
    "through": "~2.3.4",
    "jquery":"~1.11.0",
    "highcharts-browserify": "~0.0.0-3.0.10"
  },
  "devDependencies": {
    "tape": "~2.12.3",
    "browserify": "^3.44.2"
  }
}
